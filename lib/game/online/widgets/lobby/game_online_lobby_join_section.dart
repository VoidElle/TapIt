import 'package:flutter/material.dart';

class GameOnlineLobbyJoinSection extends StatelessWidget {

  const GameOnlineLobbyJoinSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      "LOBBY JOIN"
    );
  }

}
